{
  "name": "project-os",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "test": "bash tests/run-all.sh",
    "test:fast": "bash tests/run-all.sh --fast",
    "test:unit": "node --test \"tests/**/*.test.ts\"",
    "test:only": "bash tests/run-all.sh --only"
  }
}
