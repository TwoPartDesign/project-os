Audit of #T189 complete: no secrets or injection, but two HIGH correctness bugs — `--json` silently truncates through a pipe, and the threshold simulator accumulates context across cycles/files, inflating the very numbers the knowledge doc's recommendation rests on. All 8 tests pass; worktree left clean (`git status --porcelain` empty).

HIGH / scripts/compaction-metrics.ts:709-713 / BUG: `process.exit(0)` after `process.stdout.write` truncates piped stdout. Probe on a 30k-turn fixture: `--json > file` = 10,132,398 bytes; `--json | wc -c` = 65,536 bytes, exit 0, `JSON.parse` fails. This is the usage documented in metrics.md. / Delete `process.exit(0)`.
HIGH / scripts/compaction-metrics.ts:414-438 / BUG: `running` is never reset per cycle or per file; each cycle's first turn adds its full re-seed context (~86,783 per the doc) on top. Probe: 3 identical cycles peaking at 260k report 2 compactions at a 280k threshold; two unrelated session files in one directory report 1. Doc tables 80-98 are inflated. / Reset `running` at each cycle/file start, or add only the summary delta.
MEDIUM / scripts/compaction-metrics.ts:683-684,705-706 / BUG: `--window`/`--pct` only checked for finiteness. Probe: `--window -350000` → configured -280,000, every turn fires a compaction; `--window 0` → threshold 0. / Require > 0 (and pct ≤ 100).
MEDIUM / scripts/compaction-metrics.ts:200-201 / BUG: `uuid` fallback in the response-id chain is per-record, defeating the block collapse the header warns about. Probe: 3 blocks of one response without `message.id` → 3 turns, 600,000 cacheRead instead of 200,000. / Stop at `requestId`; warn on fallback.
MEDIUM / scripts/compaction-metrics.ts:491-500,516 / BUG: `statSync`/`readFileSync` unguarded. Missing path → raw ENOENT stack, exit 1, not the usage message; a directory entry named `*.jsonl` → `node:fs:440` EISDIR stack. / try/catch → stderr message + exit 2.
MEDIUM / docs/knowledge/compaction-metrics.md:87-98 / BUG: the "reset at 86,783" table — the one the recommendation uses — is not reproducible. `analyze()` takes only `{window, pct}` and derives postTokens from observed boundaries; there is no `--post-tokens` flag. / Add the flag or mark the table hand-computed.
MEDIUM / .claude/settings.json:72 / VULN: `Bash(node scripts/compaction-metrics.ts*)` is a prefix wildcard — it auto-approves any path argument (reads any file the user can read) and, depending on matcher version, trailing `;`/`&&` chaining. Impact is bounded because output is numeric. / Narrow toward a path-shaped suffix.
LOW / scripts/compaction-metrics.ts:511-527 / BUG: `readFileSync(...,"utf8").split("\n")` holds the whole file plus its line array, and every turn of every file stays resident; a >512MB transcript throws ERR_STRING_TOO_LONG. / Stream line-by-line.
LOW / scripts/compaction-metrics.ts:200-201,710 / VULN: `responseId` is `String(message.id ?? ...)` taken verbatim from the transcript and echoed in `--json`. Probe: an arbitrary `message.id` string appears in stdout. Markdown output is unaffected (numbers only). / Validate against `/^[A-Za-z0-9_-]+$/`.
LOW / docs/knowledge/compaction-metrics.md:21 / VULN: commits the host path and session UUID `~/.claude/projects/-home-user-project-os/696f7242-0a63-51a0-b689-466df7a7fc6f.jsonl`. / Use `<slug>/<session>` as line 16 already does.
LOW / scripts/compaction-metrics.ts:148 / BUG: `isMainThread` also requires `agentId === undefined`, which the spec does not; a future main-thread `agentId` would silently drop every turn. / Test `isSidechain` only.

- CONCERN: the doc's "validates itself ... reproduces 3 compactions" claim is circular — the session's peak was 263k, below the 280k threshold, so those 3 come entirely from the cross-cycle accumulation in finding 2, not from independent agreement. | File: docs/knowledge/compaction-metrics.md:96-98
- CONCERN: `pinCompactionPoint` matches boundaries to turns by timestamp string equality; two boundaries sharing (or missing) a timestamp both resolve to the first `findIndex` hit, and with an empty `b.timestamp` the fallback loop never breaks and returns the last turn of the file. Single-boundary probes resolved correctly. | File: scripts/compaction-metrics.ts:472-483
- CONCERN: `analyze` segments cycles per file but runs `simulateThreshold` and `pinCompactionPoint` over the concatenated `allTurns`/`allBoundaries`, mixing unrelated sessions in directory mode. | File: scripts/compaction-metrics.ts:511-527,540-544,574
- CONCERN: CLAUDE.md says compact-suggest.sh nudges at 60%; the doc says 65%. The doc is right (`compact-suggest.sh:106`, COMPACT_PCT-15); CLAUDE.md is stale. Out of scope for this diff. | File: docs/knowledge/compaction-metrics.md:10-11
- PASS: no hardcoded secrets, tokens, or credentials; no `eval`, `Function`, `child_process`, or dynamic import in the script.
- PASS: no prototype pollution — tool-use ids go into a `Map`, decile buckets are integer-indexed, and `JSON.parse` `__proto__` keys are own properties only.
- PASS: malformed input is handled — non-JSON lines, blank lines, non-object records, and non-numeric/missing `usage` fields all fall through `try/catch` and `num()` to 0 (verified by the suite's "not json at all {" fixture).
- PASS: `renderMarkdown` emits only `n()`-formatted numbers and the fixed `cutBy` enum — no transcript message text, tool inputs, or file paths reach the markdown output, so the committed knowledge doc leaks no prompt content.
- PASS: decile bucketing edges are correct — context 0→0, 35,000→1, 349,999→9, 350,000→9 (clamped), 700,000→9; zero-sample deciles and zero-turn cycles return rate 0 with no division by zero (`window > 0` and `turns > 0` guards).
- PASS: per-response block collapse survives interleaving — three records sharing one `message.id` split by a `user` tool_result record still yield 1 turn at 200,000 context.
- PASS: `node --test tests/compaction-metrics.test.ts` → 8 pass, 0 fail, 228ms. Note the CLI test's 3-turn fixture is far under the 64KB pipe buffer, which is why finding 1 escapes it.
