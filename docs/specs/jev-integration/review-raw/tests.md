Outcome: 75/75 tests pass, but the consumer applies `decide()`'s per-question heuristic fallbacks and declined-chunk answers as if they were Jev answers (untested), `--calibrate` prints its table twice from the CLI, and several design-named tests and threshold boundaries are absent.

Test run (`node --test tests/decide.test.ts tests/egress-guard.test.ts tests/review-triage.test.ts`): `# tests 75 / # pass 75 / # fail 0 / # duration_ms 1538.443848`.

HIGH / scripts/review-triage.ts:479-508, scripts/lib/decide.ts:562 / ISSUE: `applyAnswers` merges every result's answers once any chunk has `backend==="jev"`; a chunk that declined (timeout/network) or a question rejected as malformed carries heuristic stubs (noul 0.5, choice "in_diff") that clear the consumer's heuristic pairing (0.5 < 0.85 → `duplicate_of=null`) and flip scope to `in_diff` unconditionally; `declined` is never consulted / skip results with `backend==="heuristic"` and answers whose name-level validation failed (expose rejected names on `DecisionResult`), then add test: setup two chunks, one result declined "timeout" + one jev; action applyAnswers; assert declined chunk's rows keep heuristic `duplicate_of`/`in_scope`
MEDIUM / scripts/review-triage.ts:892,1047 / ISSUE: `runTriage` writes the calibration table to stdout itself, then `main()` writes the returned `table` again → `--calibrate` from the CLI prints everything twice; also spams test output (visible in the run above) / drop the write at :892 and let `main()` own stdout; add CLI test: setup fixture, action run with `--calibrate`, assert stdout contains the header exactly once
MEDIUM / scripts/review-triage.ts:782-971 / ISSUE: `runTriage` is 190 lines mixing report loading, decide fan-out, calibration, output scrubbing, logging, and file writes / extract `loadFindings()`, `runCalibration()`, `scrubOutputRows()`
MEDIUM / scripts/lib/decide.ts:441-612 / ISSUE: `decide()` is 172 lines / extract `postToJev()` (fetch + status→reason mapping) and `validateResponse()` (doc→answers/usage)
MEDIUM / scripts/review-triage.ts:677-708 vs 493-517 / ISSUE: `computeAgreement` re-implements the three threshold rules of `applyAnswers` by hand; the two can drift silently / extract `thresholdedDup/Scope/Severity(answer, thresholds)` helpers and call them from both
MEDIUM / scripts/lib/decide.ts:486, tests/decide.test.ts:265-267,595-604, tests/review-triage.test.ts:120-122 / ISSUE: `decide()` hardcodes `getProjectRoot()` for the guard so Jev-path tests stage under the real repo's `.claude/logs/jev-test-*` and create a symlink in the real `.claude/logs`, contrary to the design ("no test touches this repo's `.claude/logs/`") and the Copied-Project-Root pattern / add `projectRoot?` to `DecideDeps` (runTriage already takes one) and stage in a temp root
MEDIUM / tests/review-triage.test.ts:702-857 / ISSUE: no threshold-boundary tests: noul exactly 0.85, `probabilities.unrelated` exactly 0.8, confidence exactly 0.8 (all `>=` inclusive); no test that `unrelated` below threshold keeps the heuristic scope, nor that `in_scope_p` is set / add setup single finding, action applyAnswers with the exact boundary value, assert merged/applied
MEDIUM / tests/review-triage.test.ts:781-817 / ISSUE: `scoreToLevel` half-rounding tested only at 1.5; 0.5→LOW, 2.5→HIGH, 3.0→CRITICAL, and clamping of <0 / >3 untested / parametrize over those scores and assert the level
MEDIUM / tests/decide.test.ts / ISSUE: design-named cases missing: `decide_scrubSubprocessNonZero_declinesScrubFailed` (impl at egress-guard.ts:302-308 ignores a nonzero scrub status — either the design or the guard is wrong; pin one), non-JSON body, `answers` absent, a question missing from `answers` (count mismatch), all answers rejected → `declined:"malformed-response"` with `backend:"heuristic"`, `readJevConfig` file-present-but-no-jev-block and unparseable JSON / one test each: setup stub response, action decide, assert `declined`/`backend`/answer deep-equals heuristic
MEDIUM / tests/review-triage.test.ts:423-527 / ISSUE: no zero-findings run (empty/missing review-raw → `chunks=[]`, `results=[]`, `declined=undefined`) and no missing-`extraReviews`-file case (`readFileSync` at review-triage.ts:812 throws uncaught, CLI dies with a stack trace) / add setup empty spec dir, action runTriage, assert `findings:[]`, `declined===undefined`; add setup bogus calibrate path, assert a usage-style error not a throw
MEDIUM / tests/review-triage.test.ts:326-349 / ISSUE: Jaccard pairing tested only below threshold; the `>= 0.6` branch (review-triage.ts:227) and its exact boundary are never exercised / add setup two findings in different files with 3-of-5 shared tokens, action heuristicCandidates, assert pair emitted
LOW / scripts/review-triage.ts:227,452,335 vs 443 / ISSUE: magic `0.6` Jaccard threshold, hardcoded `3` in `scoreToLevel`, and `["LOW","MEDIUM","HIGH","CRITICAL"]` literal duplicated before `SEVERITY_LEVELS` is declared / `const JACCARD_DUP_THRESHOLD = 0.6`; `SEVERITY_LEVELS.length - 1`; reuse `SEVERITY_LEVELS` in `severityQuestion`
LOW / scripts/lib/decide.ts:500, scripts/lib/egress-guard.ts:147-148 / ISSUE: `/ 4` chars-per-token estimate and the 24/4.0 entropy floor are unnamed; `egress-allowlist.json` records `entropy>=4.0` with no test tying it to the code / `CHARS_PER_TOKEN`, exported `ENTROPY_MIN_LEN`/`ENTROPY_MIN_BITS`, and extend `egressAllowlist_*` to assert the guard string
LOW / scripts/review-triage.ts:408-415 vs 426-432 / ISSUE: question-map construction for a finding is duplicated in the "extend current chunk" and "new chunk" branches of `buildQuestions` (67 lines) / extract `questionsFor(f, hiPartners)`
LOW / scripts/review-triage.ts:483-484 vs 853-854 / ISSUE: `Object.assign` merge of all results' answers duplicated / `mergeAnswers(results)`
LOW / scripts/review-triage.ts:27, tests/review-triage.test.ts:27 / ISSUE: unused imports `type Question` and `applyHeuristic` / remove
LOW / scripts/review-triage.ts:786 / ISSUE: `runTriage` accepts `opts.jsonOnly` but never reads it (only `main` does) / drop the option or honour it
LOW / scripts/review-triage.ts:58,254; scripts/lib/egress-guard.ts:2-3; scripts/lib/decide.ts:106 / ISSUE: stale "in a later task"/"A later task appends" comments now that T185/T186 shipped / reword to describe current behaviour
LOW / scripts/review-triage.ts:849 / ISSUE: `declined` reports only `results[0]?.declined`; with several chunks and mixed reasons the header is misleading / record per-chunk reasons or the first non-undefined
LOW / scripts/review-triage.ts:1056 / ISSUE: `main()` promise unhandled; a throw inside `runTriage` surfaces as an unhandled rejection stack trace instead of a message + exit code / `main().catch(...)` → stderr line + `process.exit(1)`
LOW / scripts/lib/decide.ts:150,155,182 / ISSUE: `readJevConfig` returns the shared `DEFAULT_JEV_CONFIG` object by reference; a caller mutating it changes the module default (intended fallbacks otherwise correctly distinguished from swallowed errors — all bare catches here, in `defaultLogger`, `resolveEgressDir`, and `guardEgressFields` are the design's fail-open/fail-closed contracts) / return a structured clone
LOW / tests/review-triage.test.ts:63-73,149-159 / ISSUE: `DISABLED_CONFIG`/`ENABLED_CONFIG` hand-copy `DEFAULT_JEV_CONFIG` / `{ ...DEFAULT_JEV_CONFIG, enabled: true }`
LOW / tests/review-triage.test.ts:131-146 vs tests/egress-guard.test.ts:256-274; tests/review-triage.test.ts:120 vs tests/decide.test.ts:265; :58 vs decide.test.ts:38; :864-869,935-940,990-995 vs `setupCliFixture` / ISSUE: fixture builders (`copyScannerInto`/`withCopiedScannerRoot`, `freshEgressDir`, `freshTempDir`, fixture-copy blocks) duplicated across the three test files / move to `tests/helpers/egress-fixtures.ts`
LOW / tests/egress-guard.test.ts:307-310; tests/review-triage.test.ts:922-925,957-960 / ISSUE: vacuous/loose assertions: `redactions >= 0`, `redactions >= 1`, `table.includes("0.9")` / assert the exact count (1) and the exact cell `| 0.90 |` on the `security-1` row
LOW / tests/decide.test.ts:376-409 / ISSUE: `decide_timeout_declinesTimeout` depends on wall-clock (`timeout_ms: 10`, `duration_ms < 1000`) plus a 5 ms keep-alive interval — flaky under load / inject `now` and drive the abort from the stub instead of a real timer
LOW / tests/decide.test.ts:226-262 / ISSUE: `threeQuestions`/`happyDoc` are module-level mutable objects shared by 15 tests (rule: no shared mutable state) / `Object.freeze` or build per test via a factory
LOW / tests/decide.test.ts:629,686,738,189; tests/review-triage.test.ts:530,703,895; tests/egress-guard.test.ts:510 / ISSUE: names off the `[unit]_[scenario]_[expected]` form: two-part (`decide_bodyBuiltFromGuardedFields`, `decide_keyNeverInBodyLogsOrResult`, `decide_logsQueriedEventWithMetadata`, `decide_logsDeclinedEventWithReasonAndThresholds`), four-part (`applyAnswers_dupAboveThreshold_merged_belowCleared`, `runTriage_outputJson_isScrubbed_evenOnHeuristicPath`, `guardEgressFields_defaultDeps_egressDirNotPreexisting_createsWith0700`), and `runTriage_fixture_directCall_matchesCliOutput` never compares against CLI output / rename to three segments; either diff against the CLI JSON or rename to `_returnsHeuristicRows`
LOW / tests/decide.test.ts:629-657 / ISSUE: design's `decide_denylistKey_redactsValueNotRefuses` also asserts the prose word "authorization" survives; the shipped test omits that half / add the prose token to `state` and assert it is present in the body
LOW / scripts/lib/decide.ts:111-113, scripts/lib/egress-guard.ts:197-199, scripts/review-triage.ts:791-793 / ISSUE: the `scrubCmd`/`scanCmd`/`egressDir` dep triple is declared three times / `Pick<GuardDeps, "scrubCmd"|"scanCmd"|"egressDir">`
LOW / tests/review-triage.test.ts:684 / ISSUE: `160000` literal duplicates the unexported `CHUNK_SIZE_LIMIT` / export the constant and import it
LOW / scripts/review-triage.ts:425-433 / ISSUE: a single finding whose block alone exceeds `CHUNK_PACK_LIMIT` is still pushed as its own chunk, so the "never exceed 160k" docstring claim is not enforced / assert-or-truncate, and add a test with one 200k-char issue
SUGGESTION: expose which question names `decide()` rejected (e.g. `rejected: string[]`) so `applyAnswers` can skip them precisely rather than by chunk | File: scripts/lib/decide.ts:585-587
SUGGESTION: the test-level containment coverage (symlink-out, symlink-in-root sibling, prefix collision) is good; mirror it once through `decide()` after adding `projectRoot` to its deps | File: tests/egress-guard.test.ts:398-469
PASS: every exported function and type in the three modules carries a docstring; zero runtime dependencies (only `node:` builtins imported).
PASS: security-guard tests cover the in-bounds indirection cases (symlink to in-root sibling, prefix-collision path) and the fail-closed paths (scrub exit 0 but scan fails, line-count mismatch, scrub throws with cleanup verified).
PASS: fetch, scanner subprocesses, logger, and clock are injected; no internal module is mocked; every test builds its own temp fixture in a `try/finally` and no `beforeEach` is used.
PASS: `SENSITIVE_KEY_RE` textual parity with `scripts/observation-parser.ts:217` is asserted by reading the source; `JEV_ENDPOINT` host/caller/endpoint tied to `egress-allowlist.json`.
PASS: field whitelist enforced via `pick()` and asserted (`buildQuestions_bodyContainsOnlyWhitelistedFields`); API key asserted absent from body, result, and logs.
PASS: `tests/security-scanner.test.ts` diff adds the three design-named `bare-sk-token` cases with exact `ruleId`/count assertions and runtime-joined fixtures.
