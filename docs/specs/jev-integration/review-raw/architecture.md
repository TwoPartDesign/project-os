Outcome: jev-integration matches the plan on every acceptance criterion I could grep or run (75/75 tests in the three suites, test-rules 12/12 incl. bare-sk-token, shipped-settings 4/4, validate-roadmap OK, no new HIGH in system-map report); one confirmed CLI bug, several LOW header/log fidelity gaps, and out-of-feature scope creep on the branch.

MEDIUM / scripts/review-triage.ts:892,1047 / DRIFT: T186 says `--calibrate` prints the table and exits 0; runTriage writes it at :892 and main() writes the returned table again at :1047 (`if (args.calibrate || !args.jsonOnly)`), so the CLI prints the calibration table twice (probe in a copied root: "calibration table header printed 2 time(s)"; the unit test calls runTriage directly and never sees it) / drop the `args.calibrate ||` clause or the write at :892
MEDIUM / docs/maps/system-map.md:198-214 / DRIFT: design wires review-triage.ts → decide.ts, but the map has no `s_review_triage --imports--> l_decide` or `s_decide_test --imports--> l_decide` edge; `TS_IMPORT_RE` (scripts/lib/system-map-lib.ts:330) is line-anchored and both files use multi-line `import { ... } from "./lib/decide.ts"` (review-triage.ts:21-34), so `l_decide` shows "1 dependent" and the sole consumer is invisible / extend extractTsImports to join multi-line import statements (pre-existing extractor gap, surfaced by this feature)
LOW / scripts/lib/decide.ts:589-602 / DRIFT: T185 says a partial rejection sets `declined: "malformed-response"`; the result does, but the `jev-queried` kv has no `declined`/`reason` key, so the audit log cannot distinguish a clean from a partially-rejected response (the T185 completion-report deviation describes only the event name) / add `declined: "malformed-response"` to the jev-queried kv when anyRejected
LOW / scripts/review-triage.ts:846-849,954-962 / DRIFT: design's review-triage.json header carries `declined`; with zero findings no chunk is built, decide() is never called, and the header omits `declined` entirely (probe: `{"backend":"heuristic","findings":0}`); with multiple chunks only `results[0].declined` is reported / call decide once on an empty question map or derive `declined` from all results
LOW / scripts/review-triage.ts:82-90 / DRIFT: T184 says `file:lines` splits at the last `:` with `lines=""` only when there is no `:`; splitFileLines also keeps the whole token as `file` when the suffix is not `N`/`N-M` (Windows `C:/x` safe, but a `foo.ts:abc` line silently becomes file `foo.ts:abc`) / accept as a refinement or document it in the docstring
LOW / scripts/review-triage.ts:497-509 / DRIFT: design says applyAnswers "replaces a heuristic value with the Jev value only above the matching threshold"; a non-`unrelated` scope choice is applied with no threshold (fixture: architecture-2 heuristic `unrelated` → Jev `in_diff`), which is a defensible reading of `out_of_scope_p` but is the source of `scope_changed: 1` in the lift record / confirm intent; if in_diff/adjacent should also be gated, gate on `probabilities[choice] >= out_of_scope_p`
LOW / scripts/review-triage.ts:915-921,941 / DRIFT: T186 says header `redactions` = the output guard's count; on refusal every field counts as a redaction (`outputRedactions = outboundFields.length`) and decide's per-chunk redactions are summed in too, so the number mixes wire and local counts / document or split into `redactions` and `withheld`
LOW / scripts/lib/egress-guard.ts:244-247 / DRIFT: T183 specifies `execFileSync("node", [...])`; implementation uses `process.execPath` (more robust, but not the brief's shape) / accept; note in the completion report
LOW / scripts/lib/decide.ts:248-252 / DRIFT: T179 signature is `defaultLogger(event, kv)`; a third `cwd?` parameter was added for the T179 test / accept (test-driven, harmless)
LOW / docs/specs/jev-integration/design.md:78,246 / DRIFT: design cites `scan-rules.js:1620`, `:824`, `:2551` and tasks.md `:2546-2565`; the prettier reformat moved every rule (file 2570 → 3021 lines; bare-sk-token now at :2995), so those anchors are stale / none required for code; a note in the ADR would help

UNPLANNED: `scripts/lib/scan-rules.js` — whole-file prettier reformat (single→double quotes, trailing commas, line wraps); normalized diff (whitespace/quotes/trailing commas stripped) shows the ONLY substantive change is the appended `bare-sk-token` rule, rule count 233→234 | Risk: low functionally (test-rules 12/12, no regex changed), medium for blame/line-anchor churn on a 3k-line security file
UNPLANNED: `.claude/rules/bash.md:12-22,71` + `docs/knowledge/decisions.md:262-273` (ADR "Dedicated Tools Over Shell", commit 60811e4) — not in tasks.md or design File Changes | Risk: low, governance text only, but it rides the feature branch
UNPLANNED: `ROADMAP.md:313` — #T189 compaction-metrics task filed and dispatched `[-]` on this branch (commits fb06924, 6f4bc7a, 3aec993) | Risk: low; unrelated feature state mixed into the jev-integration PR
UNPLANNED: `.claude/hooks/log-activity.sh:9` gained `review-triaged` and `metrics.md` a lift grep — covered by the accepted lift-record scope | Risk: none

PASS: T178 rule at scan-rules.js:2995-3017 matches the spec regex/allowlist/testCases; three scan-files tests at tests/security-scanner.test.ts:341,362,378
PASS: T179 `process.env` read only inside decide() (decide.ts:447); JEV_ENDPOINT constant, not read from settings/env; readJevConfig strict-boolean and [0,1] fallbacks
PASS: T185 `grep -n "fetch("` → exactly one call site (decide.ts:507); redirect:"manual", AbortSignal.timeout, 3xx→redirect, non-2xx→http-<status>, AbortError/TimeoutError→timeout, else network; error objects never logged; every DeclineReason has a test (decide.test.ts:165-679)
PASS: T180/T183 egress-guard imports only node:crypto/fs/child_process/path; realpath+sep containment, wx/0600 staging, scrub status ignored, line-count check, positive scan-files re-scan, finally-cleanup; prefix-collision and in-root-symlink tests present; probe shows empty `.claude/logs/jev` after a run
PASS: fail-open/fail-closed — every decline returns heuristicBackend() (decide.ts:455-480); guard refusal never sends; heuristic always answers
PASS: T181 settings.json diff is exactly the permission line (after skill-ledger) and the jev block; egress-allowlist.json has every key from tasks.md incl. `endpoint`, `risk_level`, `audit_date: 2026-09-20`; parity test egressAllowlist_containsEndpointHostAndCaller passes
PASS: T182 fixtures — 7 severity lines, dup pairs A1/S1 and S2/Q2, S3 out-of-scope, Q1 ` / ` inside ISSUE, `ghp_` token in S2 FIX; heuristic-path output is scrubbed (probe: S2.fix → `[REDACTED:github-pat]`, header redactions 1)
PASS: T184 isMain guard (review-triage.ts:1051-1057); parseFindings last-separator rule; Jaccard 0.6; scope in_diff/adjacent/unrelated
PASS: T186 buildQuestions reads findings only via pick() (:283-308); dup_/scope_/sev_ names; chunk limit 160k with 150k pack margin; liftSummary + `lift` header (null on heuristic path) + `review-triaged` event; --calibrate writes review-triage-calibration.json and does not write review-triage.json
PASS: T187 review.md step 0 (:188-202) names the exact command, "advisory", gitignored review-raw, and the lift quote; `${BASE}` is defined at review.md:15; log-activity.sh:9 lists the three events; metrics.md has both greps and the values are string-quoted by log-activity.sh:60 so the greps match
PASS: T188 architecture.md rows for decide/egress-guard/review-triage + 234-rule and egress-allowlist/egress-guard bullets; decisions.md ADR with Key-shape check, Calibration procedure, `### Calibration record` table and placeholder row; manifest.json lists all four files (:78,117,119,120); new-project.sh:363 and generate-manifest.sh:193 ship review-triage.ts; shipped-settings 4/4; validate-roadmap OK
PASS: No TODO/FIXME/HACK in any feature file (only pre-existing decisions.md:132 and the review.md checklist text); no new npm dependency (imports are node:* and ./lib only)
PASS: system-map report — no HIGH findings; the 8 MEDIUM orphan-script findings are pre-existing and name none of the feature's files

Not verified: `bash tests/hook-smoke.sh` (T187) — outside the three suites the brief allowed; the hook change is comment-only.
