Security audit of jev-integration complete: no CRITICAL/HIGH; three MEDIUM guard gaps (denylist shape coverage, hex-charset entropy blind spot, `scan:allow` inline-marker bypass) plus LOW hardening items; all cited suites pass (75/75 + 11/11).

MEDIUM / scripts/lib/egress-guard.ts:94-111 / VULN: denylist redaction misses `KEY="value"` (quote excluded from value class), `key = value` (whitespace around `=`), and `key: value` (colon) shapes; probe: `TOKEN="abc123def456"`, `api_key = '...'`, `privateKey: abcdefghij1234567890` each returned 0 redactions, so low-entropy secrets in the commonest finding-prose shapes reach the wire / allow optional whitespace, a `:` separator, and quoted values in both regexes
MEDIUM / scripts/lib/egress-guard.ts:145-160 / VULN: entropy floor 4.0 bits/char is unreachable for hex-charset tokens (max log2(16)=4.0); probe through the real guard: 32/40/64-char hex keys scored 3.64/3.90/3.67 and passed scrub, re-scan, denylist and entropy (`hex leaks: true`) / use a length/charset-aware bar (as scanner's effectiveEntropyThreshold) or add a `[0-9a-f]{32,}` redaction
MEDIUM / scripts/lib/egress-guard.ts:305,325 (scanner behaviour at security-scanner.ts:379) / VULN: a staged field containing the scanner's inline marker `scan:allow` is skipped by BOTH scrub and the positive re-scan, so "verified clean" is vacuous for that line and only the entropy floor remains; probe: ghp_ token on a `scan:allow` line came back `[REDACTED:entropy]` not `[REDACTED:github-pat]`; a hex/low-entropy secret on such a line leaks; reviewer reports in this repo plausibly quote `scan:allow` / neutralize the marker in fields before staging, or run the subprocess with `--allowlist` pointing at a file whose `inline.marker` cannot occur
LOW / tests/fixtures/review-raw/security.md:4 / VULN: literal GitHub-PAT-shaped token (exact 36-char class) committed; invisible to the scanner only because `tests/fixtures/**` is path-ignored (allowlist.json:10) / obviously fake; optionally build it at test time by concatenation
LOW / scripts/review-triage.ts:903-904,933-937,583 / VULN: local review-triage.json and stdout table guard only `issue`/`fix`; `file`, `lines`, `id` are written unscrubbed though the design says every text field is the scrubbed value / add `file` to outboundFields
LOW / scripts/lib/decide.ts:494-498 / VULN: `config.model` (settings-controlled string) is serialized into the body without passing the guard; a settings edit can carry arbitrary text to the vendor / validate `model` against `^[A-Za-z0-9._-]{1,64}$` in readJevConfig
LOW / scripts/lib/decide.ts:371-383,406-411 / VULN: `probabilities` values only checked finite, not [0,1], and score `probabilities` accepts unbounded keys; a value >1 satisfies review-triage.ts:504 and is written as `in_scope_p` / require isUnitInterval per probability; require score keys within 0..n-1
LOW / .claude/settings.json:71 / VULN: `Bash(node scripts/review-triage.ts*)` auto-approves any argv; `<spec-dir>`, `--changed-files`, `--calibrate` paths are unconstrained (review-triage.ts:801,812,818,887,965), so an auto-approved command can read any file (its lines go outbound via the guard when Jev is on) and drop review-triage.json into any writable dir / accept as consistent with sibling entries, or constrain paths under docs/specs in main()
LOW / scripts/lib/egress-guard.ts:222-225,297 / VULN: TOCTOU — realpath containment checked on the directory, file created later with `wx`; swapping `.claude/logs/jev` for a symlink in between lands the staging file outside root (same-user attacker only) / re-verify realpath of the created file before scrub
LOW / scripts/lib/egress-guard.ts:222 / VULN: mode 0o700 applies only when the dir is created; a pre-existing wider-mode `jev/` is never tightened / chmodSync(real, 0o700) after mkdir
LOW / scripts/lib/egress-guard.ts:244-248 / VULN: scanner subprocess has no `timeout`; a hung scanner hangs the triage run (availability only; fail-closed) / pass `timeout` to execFileSync
LOW / scripts/lib/decide.ts:536,163 / VULN: `response.json()` has no size bound and is limited only by `AbortSignal.timeout`, while readJevConfig accepts any finite non-negative timeout_ms (e.g. 1e12) / cap timeout_ms and read the body with a byte cap
LOW / scripts/review-triage.ts:812 / VULN: `--calibrate` extra paths read without existsSync; ENOENT stack trace exposes only the path / existsSync + warn

CONCERN: entropy floor minLen 24 leaves any prefix-less secret under 24 chars uncaught by all three guards (design-accepted) | File: scripts/lib/egress-guard.ts:147
CONCERN: allowlist `data_classes` omits `finding.id` and `model`, both of which reach the body (id is derived from reviewer name; model from settings) | File: .claude/security/egress-allowlist.json:8-15
PASS: no hardcoded secrets in decide.ts/egress-guard.ts/review-triage.ts; test tokens built by concatenation (decide.test.ts:689-691, egress-guard.test.ts:290,322, review-triage.test.ts:915, security-scanner.test.ts:46,50)
PASS: every subprocess is execFileSync with an argv array (egress-guard.ts:244, decide.ts:254); staging path is `egress-<pid>-<6 random bytes>.txt`, never user-supplied
PASS: egress-dir containment is realpath + separator-anchored (egress-guard.ts:225); tests cover symlink-outside, symlink-to-in-root-sibling, prefix-collision (egress-guard.test.ts:398,425,449); prefix-collision `<root>-evil` refused
PASS: `redirect: "manual"` and 3xx decline (decide.ts:514,524); endpoint is a constant; allowlist host/caller tied by decide.test.ts:772
PASS: API key appears only in the Authorization header; fetch errors reduced to `err.name` (decide.ts:517-521); jev-queried/jev-declined/review-triaged carry only counts, reason, thresholds, basename (decide.ts:460-472,589-602; review-triage.ts:943-952); log-activity.sh JSON-escapes values
PASS: scan-rules.js diff is prettier reformatting plus one added rule (probe over both modules: 233→234 rules, only ADDED bare-sk-token, no CHANGED/REMOVED, ENTROPY_THRESHOLD unchanged); regex is linear (200k-char adversarial input 3.7 ms), matches `sk-`+24 class, excludes sk-ant-/sk-proj-, no match on sk_live_
PASS: staging file 0600 `wx`, `.tmp`/`.tmp.bak` removed in finally; no residue in `.claude/logs/jev` after the real-scanner probe
PASS: scan-files exits 2 on zero scanned files / path outside root and 1 on findings (security-scanner.ts:478-480,683-691), so the positive re-scan is fail-closed for unreadable or rejected paths
PASS: answers validated per question (type, criteria membership, [0,1]/[0,n-1] range); extra answers ignored; usage fields checked finite; refusal writes `[WITHHELD:scrub-failed]` locally
PASS: `node --test tests/decide.test.ts tests/egress-guard.test.ts tests/review-triage.test.ts` → 75 pass / 0 fail; `tests/security-scanner.test.ts` → 11 pass / 0 fail (run in the merged worktree)

Out of scope: `git rev-parse --show-toplevel` (scanner root) is realpath'd while `getProjectRoot()` (cwd-based) is not, so a project root reached through a symlink makes the guard always refuse `scrub-failed` (availability, fail-closed). Working tree untouched beyond the merge; probe scripts live in the scratchpad.
